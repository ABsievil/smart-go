export enum UploadMimeType {
    JPG = 'image/jpeg',
    PNG = 'image/png',
    WEBP = 'image/webp',
}

export enum UploadFolder {
    AVATARS = 'avatars',
    BANNERS = 'banners',
    DOCUMENTS = 'documents',
    OTHERS = 'others',
}

export enum CloudinaryResourceType {
    IMAGE = 'image',
    VIDEO = 'video',
    RAW = 'raw',
    AUTO = 'auto',
}
